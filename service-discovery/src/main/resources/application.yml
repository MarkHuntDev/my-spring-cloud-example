server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false # Don't register with Eureka service
    fetchRegistry: false # Don't cache register information locally when Eureka starts
  server:
    enableSelfPreservation: false